# JOJO Recompiled migration bootstrap

This repository is the writable continuation target for the PS1 native-runtime work. The canonical source baseline is being imported from `matheuz232/JOJO-Recompiled`, then the approved direct-disc runtime milestones are applied on top.
