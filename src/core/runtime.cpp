#include "core/runtime.h"

namespace jojo {

// The shipping runtime is direct-disc only. Its entry points live in
// ps1_direct_runtime.cpp so the original user-supplied PS1 image remains the
// authoritative data source at runtime.

} // namespace jojo
